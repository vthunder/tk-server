#
# Queries & Mutations
#

type Query {
  ping: String
}

type Mutation {
  mailing_list_signup (name: String, email: String!, list: String): String
  create_coupon_token (type: String): String
  use_membership_coupon (coupon: String!): String
}
